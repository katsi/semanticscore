limit_req_zone $binary_remote_addr zone=sparql:10m rate=10r/m;
server_tokens off;

server {
    listen 80;
    server_name knowledge.semanticscore.net;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name knowledge.semanticscore.net;

    ssl_certificate     /etc/letsencrypt/live/knowledge.semanticscore.net/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/knowledge.semanticscore.net/privkey.pem;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options    "nosniff"                             always;
    add_header X-Frame-Options           "SAMEORIGIN"                          always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin"     always;

    location /sparql {
        limit_req zone=sparql burst=5 nodelay;
        proxy_pass         http://app:5000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location / {
        proxy_pass         http://app:5000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# -----------------------------------------------------------------------
# api.semanticscore.net — separate project (kg-api), joins this compose
# stack's network (semanticscore_default) as an external network.
#
# STEP 1 (first deploy only): HTTP + ACME challenge, no cert yet.
# Run:  docker compose run --rm --entrypoint certbot certbot certonly --webroot -w /var/www/certbot \
#         -d api.semanticscore.net --email <EMAIL> --agree-tos --non-interactive
# Then: comment this block out, uncomment the STEP 2 blocks below,
#       docker compose restart nginx
# -----------------------------------------------------------------------

# server {
#     listen 80;
#     server_name api.semanticscore.net;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         proxy_pass         http://kg-api:8000;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }

# -----------------------------------------------------------------------
# STEP 2: uncomment after running the certbot command above.
# -----------------------------------------------------------------------

server {
    listen 80;
    server_name api.semanticscore.net;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name api.semanticscore.net;

    ssl_certificate     /etc/letsencrypt/live/api.semanticscore.net/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.semanticscore.net/privkey.pem;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options    "nosniff"                             always;
    add_header X-Frame-Options           "SAMEORIGIN"                          always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin"     always;

    location / {
        proxy_pass         http://kg-api:8000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# -----------------------------------------------------------------------
# views.semanticscore.net — separate project (semanticscore-web), same
# external network as api.semanticscore.net above.
#
# STEP 1 (first deploy only): HTTP + ACME challenge, no cert yet.
# Run:  docker compose run --rm --entrypoint certbot certbot certonly --webroot -w /var/www/certbot \
#         -d views.semanticscore.net --email <EMAIL> --agree-tos --non-interactive
# Then: comment this block out, uncomment the STEP 2 blocks below,
#       docker compose restart nginx
# -----------------------------------------------------------------------

# server {
#     listen 80;
#     server_name views.semanticscore.net;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         proxy_pass         http://semanticscore-web:3000;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }

# -----------------------------------------------------------------------
# STEP 2: uncomment after running the certbot command above.
# -----------------------------------------------------------------------

server {
    listen 80;
    server_name views.semanticscore.net;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name views.semanticscore.net;

    ssl_certificate     /etc/letsencrypt/live/views.semanticscore.net/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/views.semanticscore.net/privkey.pem;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options    "nosniff"                             always;
    add_header X-Frame-Options           "SAMEORIGIN"                          always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin"     always;

    location / {
        proxy_pass         http://semanticscore-web:3000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}
